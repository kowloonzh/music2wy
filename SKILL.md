---
name: music2wy
description: 按歌名或歌手从 flac.music.hi.cn 找歌、下载并上传到网易云音乐「云盘」。当用户说"帮我下载歌曲 X"、"找歌手 X 的歌"、"这首歌网易云没版权"、"下到网易云盘"、"music2wy"时使用。流程是：搜索音源 → 列出候选让用户确认 → 下载无损/320K → 写元数据与歌词 → 上传到用户自己的网易云云盘（可选加入歌单）。
---

# music2wy — 下载歌曲并上传到网易云盘

把「网易云没有版权、听不了」的歌，从聚合站 `flac.music.hi.cn` 找无损音源下载到本地，
再上传进用户自己的**网易云音乐云盘**，这样就能在网易云 App 里正常听。

## 绝对不要跳过的三条规则

1. **先给候选、等用户确认，再下载。** 用户说"帮我下载泸沽湖"时，你的任务只是**搜索并展示候选**，
   然后停下来等用户选。**不要在用户没确认前就 `get`**。用户明确说"直接下第一个/随便挑一个好版本"时才可直接下载。
2. **站点有限流（rate limit），不要反复请求。** 同一个关键词不要重复搜。搜索结果会自动缓存
   （默认 30 分钟），要重新展示候选请用 `show`（零请求），**不要**为了再显示一次而重新 `search`。
3. **回复里不要出现音源地址。** 不要贴出直链、CDN 地址、上游平台 URL 或任何 `http(s)://` 的音源链接，
   也不要整段复制脚本的原始输出（其中可能含调试信息）。给用户看的只有：候选表格（编号、歌名、歌手、
   专辑/版本、时长、可用音质、评分）、以及下载和上传的结果摘要（文件名、大小、音质、时长校验、是否已进云盘）。
   需要排查直链时用 `--verbose` 在本机看，不要把地址发到聊天里。

## 依赖检查

先跑一次自检（第一次用、或报错时）：

```bash
node $SKILL_DIR/scripts/music2wy.mjs doctor
```

- `node` 必需（>= 18，需要内置 fetch）。
- `ffmpeg`/`ffprobe` 可选：没有它照样能下载和上传，只是不写元数据/封面/歌词。
- 登录态：`~/.music2wy/cookie.json`。没登录就先做下面的登录。

**不需要浏览器。** 整套流程（含站点的人机挑战、网易云扫码登录）都是纯 Node 实现的，
所以任何装了 Node 的机器都能跑；扫码登录只需要用户的手机上有网易云 App。

`$SKILL_DIR` 是本 skill 所在目录（含 `SKILL.md` 和 `scripts/`）。用之前先设好：

```bash
SKILL_DIR="$HOME/.agents/skills/music2wy"    # 若 skill 装在别处，改成实际路径
```

## 登录网易云（需要时）

先 `whoami` 看有没有登录态：

```bash
node $SKILL_DIR/scripts/music2wy.mjs whoami
```

没登录（或报"未登录/登录态失效"）时，运行 login，它会**生成二维码并在终端里画出来，
同时把二维码存成 PNG 并用系统看图程序弹出来**，还会打印二维码链接：

```bash
node $SKILL_DIR/scripts/music2wy.mjs login
```

然后**告诉用户**：用手机上的「网易云音乐」App → 左上角菜单 → 扫一扫，扫描屏幕上的二维码并确认。
命令会自己轮询，扫码成功后把 cookie 存到 `~/.music2wy/cookie.json`，之后就不用再登录了。

> 如果用户的机器上已经装了网易云音乐桌面客户端并登录过，也可以直接用客户端登录态，
> 见 `reference/TROUBLESHOOTING.md` 里的「复用桌面客户端登录态」。

## 主流程

### 按歌手浏览歌曲

用户只给**歌手名**，想先看看有哪些歌时，用 `artist`，不要用按歌名评分的 `search`：

```bash
node $SKILL_DIR/scripts/music2wy.mjs artist "麻园诗人"
node $SKILL_DIR/scripts/music2wy.mjs artist "麻园诗人" --page 2  # 用户想看下一页时
```

站点每页最多返回 20 条。`total` 是站点搜索记录数，含不同版本与翻唱，不等于独立歌曲数。
`artist` 只保留歌手字段命中的曲目，独唱排在合作曲目前；保留不同版本供用户选择。
给用户展示**本页编号、歌名、歌手、专辑/版本、时长、音质和 warnings**；这类搜索没有
歌名参照，`score` 和 `recommended` 不作为选歌依据。用户选好本页编号后运行 `get --pick N`。
翻页会替换当前选择会话，编号从 1 重新开始；想重看当前页用 `show`，不要重复请求站点。
下载时若站点签名过期，脚本会刷新当前页并按歌曲 ID 找回同一条；找不到会停止，不会改下别的歌。
与按歌名搜索一样，**必须先展示候选、等用户确认，再下载**。

### 第 1 步：搜索

```bash
node $SKILL_DIR/scripts/music2wy.mjs search "泸沽湖"
```

带歌手名更准：`search "泸沽湖 麻园诗人"`。
（站点本身只接受**单个**关键词，脚本会自动拆解：先用歌名去搜，再把歌手名当作排序依据，
所以你按"歌名 歌手"的写法给就行。）

它会返回 JSON。看完 `candidates` 后，**用下面这个格式把候选列给用户看**（不要直接贴 JSON）：

> 在 flac.music.hi.cn 找到 **N** 个候选（已按匹配度排序）：
>
> | # | 歌名 | 歌手 | 专辑 | 时长 | 可用音质 | 评分 |
> |---|---|---|---|---|---|---|
> | 1 ⭐推荐 | 泸沽湖 | 麻园诗人 | 不爱说话的人 | 4:06 | flac/2000k, mp3/320k | 102 |
> | 2 | 泸沽湖 | 麻园诗人 | 乐队的夏天3 第2期 | 5:18 | flac/2000k, mp3/320k | 102 |
> | 3 | 泸沽湖 (新版) | 麻园诗人 | 泸沽湖 (新版) | 4:29 | flac/2000k | 102 |
>
> 默认下第 1 个的 **FLAC 无损**。回复序号确认（例如"2"），或者说"320"改音质。

注意：
- 把 `warnings` 里有过滤词（DJ/伴奏/翻唱等）的项**明确标注出来**提醒用户。
- `recommended: true` 的那条标 ⭐。
- 如果候选明显都不对（比如全是 DJ 版），告诉用户，并建议换个关键词或歌手名，**不要**硬下。
- **如果返回里 `warning` 不是 null**（意思是"站点上没有找到这位歌手的版本"），
  必须**如实转告用户**，例如：
  > ⚠️ 站点上没有周杰伦的原唱《晴天》，搜出来的都是翻唱（关诗敏、刘瑞琦…）。
  > 音源站（kuwo / wyy 两个上游都试了）拿不到这首原唱。要不要挑一个翻唱版本，或者你手上已经有文件的话我直接帮你传进云盘？

  绝不要把翻唱当原唱默默下下来 —— 那等于给用户的云盘塞了一首错的歌。
- 站点只有两个上游：`kuwo`（默认）和 `wyy`。搜不到时换另一个再试一次。**不要**试别的平台名。

### 第 2 步：等用户确认

停下来等回复。用户说"1"、"第 1 个"、"就它"之后才继续。

### 第 3 步：下载

```bash
node $SKILL_DIR/scripts/music2wy.mjs get --pick 1
```

加 `--quality 320` 可下 320K MP3（默认 FLAC 无损）。

它会：解析直链 → 下载 → 用网易云官方元数据校正歌名/歌手/专辑 → 取歌词 → 用 ffmpeg 写入标签和封面
→ 打印本地文件路径和时长校验结果。把结果汇报给用户，例如：

> ✅ 已下载：`~/.music2wy/downloads/泸沽湖 - 麻园诗人.flac`（26.4 MB，FLAC 2000k）
> 时长校验：下载 246s vs 网易云官方 246s ✅
> 元数据/封面/歌词已写入

**如果时长校验是 ⚠️（偏差 > 25%）**，说明可能下错了版本，**先告诉用户**再决定是否上传。

关于元数据和歌词，有两条重要行为你要知道：

- **元数据只信得过"核对过"的网易云条目**。脚本会拿选中的这首歌去网易云核对歌名和歌手，
  两个都对得上才用它的专辑/时长；对不上（典型：网易云没版权）就用音源站自带的元数据，
  **绝不会拿翻唱的信息覆盖**。
- **歌词会依次尝试**：网易云官方 LRC → 音源站自己的 `getLyric`。两边都没有就跳过，
  并在输出里说明（不影响下载和上传）。汇报时可以说一句"这首没找到歌词"。

### 第 4 步：上传到网易云盘

```bash
node $SKILL_DIR/scripts/music2wy.mjs upload "<上一步返回的 file 路径>" --netease-id "<上一步 meta.neteaseId>"
```

`upload` 会自己从文件名解析 `歌名 - 歌手`；也可以显式指定
`--title "泸沽湖" --artist "麻园诗人" --album "不爱说话的人"`。
**优先传 `get` 返回的 `--netease-id`**，这是网易云官方曲目 ID；没有官方 ID 时才省略。

上传内部是这几步：`upload/check` → `nos/token/alloc` → 用 LBS 找真实上传节点 → 传 NOS
→ `upload/cloud/info/v2` → `cloud/pub/v2` → `cloud/user/song/match`。**pub 必须成功**，
否则歌曲不会出现在云盘列表里；有官方 ID 时，发布成功后会自动匹配官方曲目。

自动匹配很重要：网易云会把云盘文件关联到官方曲库条目，然后才显示官方专辑、歌词和评论。
本地标签不能替代这一步。匹配结果用 `matched: true/false` 表示；失败时不影响歌曲已发布，
但要把 `matchError` 如实告诉用户。

不要用 `--no-match` 关闭匹配，除非用户明确要求保留未匹配的云盘条目。

**大文件（例如 20MB+ 的无损 FLAC）需要等服务端转码**，转码没完成时 pub 可能返回 400 或 523；
脚本会自动退避重试（最长约 3 分钟）。如果最终还是失败，返回里会带 `retryable: true`。
这时先查一次云盘列表，因为有时 `publish` 返回失败但歌曲已经可见：

```bash
node $SKILL_DIR/scripts/music2wy.mjs cloud --limit 20
```

如果云盘里已经能看到目标歌曲，就向用户说明已进入云盘，不要继续重试。若仍不可见，过一两分钟用返回的 songId 补一次发布即可：

```bash
node $SKILL_DIR/scripts/music2wy.mjs publish <songId>
```

经验规则：若同一个 FLAC 的 `publish` 连续 2 次以上仍是 `transcodeStatus=9` + `code=523`，尤其等待 5–10 分钟后仍失败，就不要反复轮询；告知用户网易云服务端转码卡住，并建议改传 320K MP3。继续高频重试通常没有帮助。

同一个文件重复上传不会产生重复条目：`upload/check` 会返回 `needUpload: false`，
脚本会从云盘里找回已有的 songId（返回 `deduped: true`）并确保它已发布。

汇报：

> ✅ 已上传到网易云盘：**泸沽湖 — 麻园诗人**（songId 1234567）
> 打开网易云音乐 App →「我的」→「我的音乐」→「云盘」就能看到并播放。

### 第 5 步（可选）：加入歌单

```bash
node $SKILL_DIR/scripts/music2wy.mjs playlist-add --name "我喜欢的" --files "<file 路径>"
```

歌单不存在会自动新建。

## 一次搞定（只在用户明确不想逐步确认时用）

用户说"你看着办，下好直接传"时：

```bash
node $SKILL_DIR/scripts/music2wy.mjs search "泸沽湖"        # 看候选，自己在心里挑
node $SKILL_DIR/scripts/music2wy.mjs get --pick 1
node $SKILL_DIR/scripts/music2wy.mjs upload "<file>" --netease-id "<get 返回的 meta.neteaseId>"
```

即便如此，也**优先**花一句话告诉用户你挑了哪个版本、为什么。

## 浏览器兜底（`--browser`，日常用不到）

正常情况下**不需要浏览器**：站点的人机挑战由脚本自己算过去。只有在下面两种情况才用得上：

- 站点换了挑战算法，纯 Node 复现失效（报"解不开挑战"）；
- 站点开始拦我们的纯 HTTP 客户端。

这时给任意命令加 `--browser`，脚本会**自动启动一次真浏览器**把挑战过掉，把 cookie 交给后面的流程：

```bash
node $SKILL_DIR/scripts/music2wy.mjs search "泸沽湖" --browser
node $SKILL_DIR/scripts/music2wy.mjs browser-session --headed   # 只解挑战，弹窗给你看
```

- 兜底顺序：**本机 Chrome（CDP，零依赖）→ Python Playwright**。可用 `--browser-path chrome|playwright` 指定。
- `--headed` 会**显示浏览器窗口**（默认无头），方便你肉眼确认到底发生了什么。
- 设计上**不做反检测伪装**（不伪造 `navigator.webdriver`、不注入 stealth 脚本）——只是"用浏览器
  访问一个你自己也能打开的网站"。如果站点就是不放行自动化浏览器，脚本会如实报错，
  **不要**再去对抗它，改用下面「手动下载 + 只上传」那条路。

> ⚠️ 一个坑：Chrome 默认走**系统代理**，而 Node 的 fetch 默认**直连**。两边出口 IP 不一致时
> WAF 可能把会话和 IP 绑定，导致"浏览器解出的 cookie 给 Node 用"失效。所以兜底时默认给
> Chrome 加了 `--no-proxy-server`；如果你的网络必须走代理才能访问该站，设
> `MUSIC2WY_CHROME_USE_PROXY=1` 关掉这个默认行为。

## 限流与缓存（重要）

`flac.music.hi.cn` 的风控很敏感。脚本内部已经做了几层保护：

- `search` 结果按「platform+关键词」缓存（默认 30 分钟），重复搜索直接走缓存并在 stderr 标 `[缓存]`。
- 解析出的直链按「歌曲+音质」缓存（默认 20 分钟）。
- 所有站点请求之间强制最小间隔（默认 **15 秒**），跨进程共享一个闸门文件。
- **每次站点请求都记账**到 `~/.music2wy/logs/requests.jsonl`，用 `stats` 可以看：

  ```bash
  node $SKILL_DIR/scripts/music2wy.mjs stats
  ```

你在使用时要配合：

- 想再显示一次候选列表 → 用 `show`，**不要**用 `search`。
- 用户改主意换关键词 → 正常 `search` 新关键词即可。
- **不要写循环去轮询站点**。探测同样算请求；站点不通时高频探测只会延长封禁。
- 遇到站点报错/返回空/一直连不上 → **停下来**，把原文告诉用户，等几分钟再试。
  要确认是"限流/封禁"还是"站点挂了"，用分层判断：
  **TCP 连得上但 HTTP 报错 → 限流；TCP 握手就超时 → IP 被丢包（封禁），只能等。**
  可调 `~/.music2wy/config.json` 里的 `minRequestIntervalMs` 加大间隔。

## 常见问题

报错排查、复用桌面客户端登录态、音质选择、平台切换等见 `reference/TROUBLESHOOTING.md`。

## 合规

仅限个人自用：把自己买不到的版本下到本地、放进自己的云盘自己听。
不要把下载的文件再分发、上传到公开平台或用于商业用途。
